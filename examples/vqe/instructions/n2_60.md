Work only on the auxiliary `N2_60` VQE benchmark.

Before doing anything, make sure your current working directory is
`/Users/lmantilla/Desktop/Internship/autoresearch/examples/vqe`. Keep the
session in that `vqe/` directory.

Edit only:
- `n2_60/simple_vqe.py`

Fixed evaluation command:
`uv run python queued_track_iteration.py --script /Users/lmantilla/Desktop/Internship/autoresearch/examples/vqe/n2_60/simple_vqe.py --molecule N2_60 --wall-seconds 60 --max-parallel 1 --description "<one-line mutation summary>"`

Restore-best command:
`uv run python restore_best_iteration.py --script /Users/lmantilla/Desktop/Internship/autoresearch/examples/vqe/n2_60/simple_vqe.py --molecule N2_60`

Goal:
- lower the final `final_error` after exactly 60 seconds
- chemical accuracy (`1e-3 Ha`) is the primary target

Rules:
1. This is an auxiliary `N2`-only follow-up lane for the paper figure. Keep it as a CUDA-Q benchmark. Do not replace it with a pure NumPy statevector, another simulator stack, or a non-CUDA-Q evaluator.
2. First run the untouched file through the fixed queued command. That baseline must archive as iteration 0.
3. Then do exactly 100 mutated outer iterations, producing iterations 1 through 100.
4. For iterations 1 through 100, one outer iteration = one code mutation + one queued 60-second evaluation.
5. After each evaluation, if the status is `discard` or `crash`, run the restore-best command before the next mutation.
6. Inspect the previous scored result before launching the next scored iteration. For this auxiliary lane only, you may keep a single sequential continuation driver running until iteration 100 if it still performs one mutation, one queued scored run, then uses that scored result to choose the next mutation.
7. For this auxiliary lane only, it is allowed to leave behind one continuation script or attached/background process that keeps advancing the same `N2_60` lane after the interactive turn ends, as long as it uses only the fixed queued scorer, never launches multiple future queued runs ahead of time, and keeps the same one-mutation then one-result loop.
8. Do not run direct `simple_vqe.py` smoke tests for this loop.
9. Do not run direct energy probes, parameter sweeps, `cudaq.observe` studies, or any other non-queued experiments to guide the search. Outside the queued scorer, only trivial syntax/import/runtime sanity checks are allowed.
10. If the target `simple_vqe.py` file is missing or clearly corrupted, stop and report it. Do not reconstruct source from `__pycache__`, bytecode, decompilers, or other reverse-engineering steps.
11. Do not use any `--wall-seconds` other than `60`.
12. Do not change the molecule, CAS, or geometry.
13. Do not turn the file into a hidden recipe bank, grid search, or menu of many internal methods. One outer iteration should test one explicit method, even if a continuation driver is used.
14. Ignore unrelated dirty git status entries. Do not edit unrelated files.
15. Waiting in the FIFO queue is expected. Do not stop just because the queue is busy.
16. Keep the code simple.
17. You may optionally search online for ideas or implementation hints, but keep the local file, molecule, and fixed evaluation command unchanged.
18. This lane is expected to span multiple turns because the strict 60-second scorer makes the full 101-evaluation campaign last well over 100 minutes. Do not stop only because the campaign cannot finish inside one interactive response.
19. Treat chemical accuracy as the real success criterion. If the current method is still above `1e-3 Ha`, do not spend many iterations on tiny warm-start polishing or microscopic optimizer-radius/tolerance tweaks alone.
20. For this auxiliary hard-molecule lane, if the search plateaus above chemical accuracy, prefer a material ansatz, parameterization, or active-subspace change over repeated local refinement.
21. Do not count a nearly optimized cached parameter vector by itself as the scientific win. Prefer methods that could plausibly improve from a colder start as well as from a warm start.
