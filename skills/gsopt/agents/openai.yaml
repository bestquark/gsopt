interface:
  display_name: "GSOpt"
  short_description: "Run fixed-budget code-mutation loops"
  default_prompt: "Use $gsopt to run an n-iteration code-mutation loop against an editable benchmark file and a fixed evaluator such as evaluate.py or evaluator.py."
